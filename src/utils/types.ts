export type labeler_options = {
    repo: string,
    token: string
}

export type label = {
    name:string,
    description?:string,
    color:string
}

export type headers = {
    Accept2: string,
    Authorization?: string
}
